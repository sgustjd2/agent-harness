# Installing agent-harness in Claude Code

**M1 placeholder.** Written in M3.

Will cover, as two clearly separated steps:

1. **Register the marketplace** — `/plugin marketplace add <org>/agent-harness`
2. **Install the plugin** — `/plugin install agent-harness@agent-harness`, then
   `/reload-plugins` if the install summary asks for it

On Claude Code both steps complete within the CLI, so the registration/installation
distinction is less visible here than on the Codex side — but the document will still
present them as two steps, because they are.

Also covered: local-path registration for restricted networks, `--plugin-dir` for
development, and how to pin a version.

**Nothing is installed today**: the seven Skills are M1 placeholders.
