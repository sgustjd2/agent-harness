# Quickstart

**M1.1 placeholder.** Written in M2, once Skills exist.

There is nothing to use yet. The installable plugin contains one compatibility fixture
Skill that does nothing by design, and no production Skill exists.

The only runnable thing in this repository is the validation suite:

```bash
python -m venv .venv
```

```bash
.venv/bin/pip install -r requirements-dev.txt
```

```bash
python scripts/validate_all.py
```

For current status: [`docs/m1-traceability.md`](m1-traceability.md).
