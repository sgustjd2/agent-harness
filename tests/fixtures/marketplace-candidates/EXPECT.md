# Expected behaviour

These three fixtures are experiment INPUTS for ATS-022, not
pass/fail cases. The experiment records host, command, exit code, output summary and
result for each candidate, on each surface, keeping ChatGPT desktop app behaviour
separate from Codex CLI behaviour. A negative result is a valid outcome.
