# Candidate A -- separate native catalogs

Two catalogs, each hand-edited. Always works, but violates PRIN-10 as a long-term
design: the two files drift (THR-020). Permitted only as a temporary scaffold.

ATS-022 records: does each host read its own catalog, and is `policy` preserved?
