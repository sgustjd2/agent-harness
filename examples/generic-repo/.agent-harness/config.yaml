# agent-harness project configuration
# Schema: plugins/agent-harness/core/schemas/config.schema.json
#
# Gates are argv arrays, never shell strings (SEC-08). Nothing here is executed
# without the user having put it here first (FR-010).

# No gates are configured. verify-work will report `unverified` until the user adds
# gates -- it never guesses commands (FR-010).
schema_version: 1
project:
  name: example-generic-repo
  type:
    - generic
vcs: git
orchestration:
  max_parallel_agents: 3
  max_delegation_depth: 1
verification:
  gates:
runs:
  retention_count: 20
  auto_prune: false
  commit_evidence: false
