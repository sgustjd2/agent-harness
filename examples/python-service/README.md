# Example: python-service

Reference `.agent-harness/config.yaml` for a python service project.
Validated against `config.schema.json` in CI, so it cannot drift from the schema.

This is documentation, not a runnable project.
